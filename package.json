{
  "name": "aerocode",
  "version": "1.0.0",
  "description": "Sistema de gestão de produção de aeronaves",
  "main": "dist/index.js",
  "devDependencies": {
    "@types/node": "^25.5.2"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "compile-start": "npm install && npm run build && npm start && echo \"isso conta como easter egg?\""
  }
}
